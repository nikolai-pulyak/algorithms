type ItemType = string|number
export function binarySearch(arr: ItemType[], val: ItemType[]): ItemType|null {
  let maxIndex = arr.length - 1, minIndex = 0, avgIndex, midd
  while(maxIndex > 0) {
    avgIndex = Math.floor((maxIndex + min)
    midd = arr[Math]
  }
}